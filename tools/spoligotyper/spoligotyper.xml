<tool id="spoligotyper" name="spoligotyper" version="@TOOL_VERSION@+galaxy0" profile="23.0">
    <description>in silico spoligotyping, species and lineage of the M. tuberculosis complex</description>
    <macros>
        <token name="@TOOL_VERSION@">0.5.1</token>
    </macros>
    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">spoligotyper</requirement>
    </requirements>
    <version_command>spoligotyper --version</version_command>
    <command detect_errors="exit_code"><![CDATA[
#import re
#if $input.type == 'single'
    #set $default_name = str($input.single.element_identifier)
#else
    #set $default_name = str($input.pair.name)
#end if
## The file type (fasta or fastq, gzipped or not) is detected from the content, not from the name
#set $name = re.sub(r'[^A-Za-z0-9._-]', '_', str($sample_name) or re.sub(r'\.(fastq|fq|fasta|fa|fna|fas)(\.gz)?$', '', $default_name)) or 'sample'
spoligotyper
    #if $input.type == 'single'
        -r1 '$input.single'
    #else
        -r1 '$input.pair.forward' -r2 '$input.pair.reverse'
    #end if
    --sample '$name'
    --output results
    --threads \${GALAXY_SLOTS:-1}
    --memory \$((\${GALAXY_MEMORY_MB:-2048} * 8 / 10))m
    #if $min_count
        --min-count $min_count
    #end if
    $no_species
    #if str($operator)
        --operator '$operator'
    #end if
    > /dev/null &&
mv results/*_spoligotyping.txt '$table' &&
mv results/*_spoligotyping.json '$json' &&
mv results/*_spoligotyping.pdf '$pdf'
    ]]></command>
    <inputs>
        <conditional name="input">
            <param name="type" type="select" label="Input data">
                <option value="single" selected="true">Single-end reads, or an assembly</option>
                <option value="paired">Paired-end reads</option>
            </param>
            <when value="single">
                <param name="single" type="data" format="fastqsanger,fastqsanger.gz,fasta,fasta.gz" label="Reads (fastq) or assembly (fasta)"/>
            </when>
            <when value="paired">
                <param name="pair" type="data_collection" collection_type="paired" format="fastqsanger,fastqsanger.gz" label="Paired-end reads"/>
            </when>
        </conditional>
        <param name="sample_name" type="text" value="" optional="true" label="Sample name" help="Default: the dataset name">
            <validator type="regex">[A-Za-z0-9._-]*</validator>
        </param>
        <param argument="--min-count" type="integer" min="1" optional="true" label="Minimum number of reads to call a spacer present" help="Default: 5 for reads, 1 for assemblies"/>
        <param argument="--no-species" type="boolean" truevalue="--no-species" falsevalue="" checked="false" label="Skip the species check and the lineage" help="Faster: one pass over the reads instead of two"/>
        <param argument="--operator" type="text" value="" optional="true" label="Operator name, shown in the PDF report">
            <validator type="regex">[^']*</validator>
        </param>
    </inputs>
    <outputs>
        <data name="table" format="tabular" label="${tool.name} on ${on_string}: table"/>
        <data name="pdf" format="pdf" label="${tool.name} on ${on_string}: PDF report"/>
        <data name="json" format="json" label="${tool.name} on ${on_string}: JSON"/>
    </outputs>
    <tests>
        <test expect_num_outputs="3">
            <conditional name="input">
                <param name="type" value="single"/>
                <param name="single" value="spacers.fasta" ftype="fasta"/>
            </conditional>
            <output name="table">
                <assert_contents>
                    <has_text text="777777777777771"/>
                    <has_text text="MTBC not detected"/>
                </assert_contents>
            </output>
            <output name="pdf">
                <assert_contents>
                    <has_size min="10000"/>
                </assert_contents>
            </output>
            <output name="json">
                <assert_contents>
                    <has_text text="spoligo_spacers"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
**What it does**

spoligotyper finds the 43 spacers of the direct repeat (DR) locus of *Mycobacterium tuberculosis* complex (MTBC)
samples in sequencing reads (fastq) or assemblies (fasta), and reports the spoligotype as binary, octal and
hexadecimal codes, and as an SB number from the Mbovis.org database. It also checks the species from regions of
difference (RD1, RD4, RD7, RD9, RD12), calls the lineage with the SNP barcode of Coll et al. (2014), estimates the fraction of
MTBC reads, and flags mixed or contaminated samples.

**Outputs**

* **Table**: the spoligotype, species, lineage, status and warnings.
* **PDF report**: results, the reads supporting each call, input files, software versions and parameters, for
  quality assurance.
* **JSON**: all the results and run information.

**More information**: https://github.com/duceppemo/spoligotyper/wiki
    ]]></help>
    <citations>
        <citation type="doi">10.5281/zenodo.22926160</citation>
        <citation type="doi">10.1128/jcm.35.4.907-914.1997</citation>
        <citation type="doi">10.1038/ncomms5812</citation>
    </citations>
</tool>
